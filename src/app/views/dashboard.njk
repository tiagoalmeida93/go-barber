{% extends "_layouts/default.njk" %}

{% block body %}
    <div class="content">
        <strong>Olá, {{ user.nome }}</strong>
        <p>Inicie um agendamento escolhendo um profissional abaixo:</p>

        <ul class="providers">
            {% for provider in providers %}
            <li class="provider">
                <div>
                    <img src="/file/{{ provider.avatar }}" alt="N/A" />
                    <strong>{{ provider.name }}</strong>
                </div>
                <a href="/app/appointments/new/{{ provider.id }}">+</a>
            </li>
            {% endfor %}
        </ul>

        {% if user.provider %}
        <a href="/app/appointments/read/{{user.id}}" class="btn">Acompanhar agendamentos</a>
        <br />
        {% endif %}
        <a href="/app/logout" class="logout">Sair</a>
    </div>
{% endblock %}